plugins {
    id 'java'
    id 'application'
}

mainClassName = 'dubbo.example.provider.Application'

dependencies {
    compile project(':dubbo-example-api')

    compile "com.alibaba.boot:dubbo-spring-boot-starter:$dubboSpringBootVersion"
}
